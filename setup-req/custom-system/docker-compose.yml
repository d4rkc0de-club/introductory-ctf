services:
  null-system:
    build: .
    restart: unless-stopped
    ports:
      - "31337:31337"
    read_only: true
    tmpfs:
      - /tmp:size=16m,noexec,nosuid
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    pids_limit: 32
    mem_limit: 64m
    cpus: 0.25
